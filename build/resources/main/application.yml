spring:
  datasource:
    url: jdbc:mysql://localhost:3306/faddy_community_feed_db
    username: root
    password: Agh@p970314
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        use_sql_comments: true
        # 성능 테스트를 위한 통계 활성화
        generate_statistics: true
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html

  mail:
    host: smtp.gmail.com  # 또는 다른 SMTP 서버
    port: 587
    username: agh0314@gmail.com
    password: pwjp apmf ivdw eqey
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
  data:
    redis:
      port: 6379
      host: localhost


# 테스트 데이터 생성 설정
app:
  test-data:
    enabled: true                  # 전체 테스트 데이터 생성 활성화 여부
    user:
      enabled: true                # 사용자 데이터 생성 활성화 여부
      count: 500000
    post:
      enabled: true                # 게시물 데이터 생성 활성화 여부
      count: 500000

# CORS 설정
cors:
  allowed-origins:
    - http://localhost:3000    # 로컬 개발 환경
    - https://your-production-domain.com  # 프로덕션 환경 (필요시 수정)

secret-key: 9a4f2c8d3b7a1e6f45c8a0b3f267d8b1d4e6f3c8a9d2b5f8e3a9c8b5f6v8a3d9asdfas

fcm:
  certification: community-service-91877-firebase-adminsdk-fbsvc-ef129dc21c.json
# SQL 쿼리 로깅
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org.springframework.jdbc.core.JdbcTemplate: DEBUG
    org.springframework.jdbc.core.StatementCreatorUtils: TRACE
